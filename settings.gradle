pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral(
                url: "https://jitpack.io"
        )
        jcenter()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral(
                url: "https://jitpack.io"
        )
        jcenter()
    }
}
rootProject.name = "aprimorandoconhecimentoss"
include ':app'
